#!/bin/sh
# No Erlang distribution / epmd or baked-in shared cluster cookie is needed.
export RELEASE_DISTRIBUTION=none
export RELEASE_TMP="${RELEASE_TMP:-/tmp/ops_brain}"
export ERL_CRASH_DUMP=/tmp/erl_crash.dump
